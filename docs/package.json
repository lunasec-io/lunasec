{
  "name": "docs",
  "version": "1.0.7",
  "private": true,
  "license": "CC-BY-SA-4.0",
  "scripts": {
    "docusaurus": "yarn run -B docusaurus",
    "start": "yarn run openapi:generate && TYPEDOC_WATCH=true yarn run docusaurus start --port 4000",
    "build": "yarn run openapi:generate && TYPEDOC_WATCH=false yarn run build:docs && yarn insert:analytics",
    "build:docs": "yarn run docusaurus build",
    "insert:analytics": "sh ./analytics/analytics-inserter.sh",
    "swizzle": "yarn run docusaurus swizzle",
    "deploy": "yarn run openapi:generate &&  docusaurus deploy",
    "deploy:s3-redirect-config": "yarn run s3-redirect-generator --inputConfig ./redirect-config.js --bucket ",
    "clear": "yarn run docusaurus clear",
    "write-translations": "yarn run docusaurus write-translations",
    "write-heading-ids": "yarn run docusaurus write-heading-ids",
    "openapi:generate": "yarn run redoc-cli bundle ../api-spec/schema/full-tokenizer.yaml --output static/tokenizer-api-spec-static/index.html"
  },
  "dependencies": {
    "@docusaurus/core": "^2.0.0-beta.9",
    "@docusaurus/preset-classic": "^2.0.0-beta.9",
    "@docusaurus/theme-search-algolia": "^2.0.0-beta.9",
    "@lunasec/s3-redirect-generator": "^1.0.7",
    "@mdx-js/react": "^1.6.22",
    "@openapitools/openapi-generator-cli": "^2.4.18",
    "@svgr/webpack": "^6.1.0",
    "clsx": "^1.1.1",
    "file-loader": "^6.2.0",
    "prism-react-renderer": "^1.2.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-is": "^17.0.2",
    "redoc-cli": "^0.13.1",
    "url-loader": "^4.1.1",
    "webpack": "^5.64.4"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@types/node": "^14.0.0",
    "docusaurus-plugin-typedoc": "^0.16.5",
    "typedoc": "0.22.10",
    "typedoc-plugin-markdown": "^3.11.7",
    "typescript": "^4.3.5"
  }
}
