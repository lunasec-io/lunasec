{
  "name": "lunasec-go-monorepo",
  "private": true,
  "version": "0.0.1-alpha.13",
  "description": "LunaSec Go Monorepo",
  "scripts": {
    "lint": "golangci-lint run",
    "format": "gofmt -w .",
    "compile:release": "make release version=\"$(npm run --silent version)\"",
    "publish:release": "make publish version=\"$(npm run --silent version)\"",
    "version": "node -p 'require(\"./package.json\").version'"
  },
  "author": "lunasec",
  "license": "MIT",
  "dependencies": {
    "@lunasec/isomorphic-common": "^0.0.1-alpha.13"
  }
}
