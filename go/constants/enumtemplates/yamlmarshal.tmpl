func (x {{.enum.Name}}) MarshalYAML() (interface{}, error) {
  string_output := x.String()

	_, err := Parse{{.enum.Name}}(string_output)

	if err != nil {
	  panic(err)
	}

	return string_output, nil
}

func (x *{{.enum.Name}}) UnmarshalYAML(value *yaml.Node) error {
  return x.UnmarshalText([]byte(value.Value))
}
