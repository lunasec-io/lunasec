package constants

const (
	OutputRegexStr = `<REFINERY_OUTPUT_CUSTOM_RUNTIME>(.*)<\/REFINERY_OUTPUT_CUSTOM_RUNTIME>`
	RuntimePath    = `/var/runtime/`
	FunctionsPath  = RuntimePath + `functions.json`
)
