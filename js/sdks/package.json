{
  "name": "lunasec-sdks",
  "version": "1.0.0",
  "private": true,
  "description": "Secure frame SDK",
  "main": "index.js",
  "scripts": {
    "schema:copy": "cd ./packages/node-sdk && yarn schema:copy",
    "schema:generate": "cd ./packages/tokenizer-sdk && yarn openapi:generate",
    "watch": "yarn schema:copy && yarn schema:generate && tsc -b tsconfig.build.json -w",
    "compile:production:sdks": "yarn schema:copy && yarn schema:generate && tsc -b tsconfig.build.json",
    "compile:production:secure-frame-front-end": "cd sdks/secure-frame-iframe && npm run compile:production",
    "compile:production": "npm run compile:production:sdks && npm run compile:production:secure-frame-front-end"
  },
  "author": "",
  "license": "PROPRIETARY",
  "devDependencies": {
    "typescript": "^4.4.2"
  }
}
