{
  "name": "lunasec-sdks",
  "version": "1.0.0",
  "private": true,
  "description": "Secure frame SDK",
  "main": "index.js",
  "scripts": {
    "compile": "yarn schema:copy && yarn schema:generate && tsc -b tsconfig.build.json",
    "schema:copy": "cd ./packages/node-sdk && yarn schema:copy",
    "schema:generate": "cd ./packages/tokenizer-sdk && yarn openapi:generate",
    "watch": "yarn run compile -- -w",
    "compile:release:sdks": "yarn schema:copy && yarn schema:generate && tsc -b tsconfig.build.json",
    "compile:release:secure-frame-front-end": "cd packages/secure-frame-iframe && npm run compile:release",
    "compile:release": "npm run compile:release:sdks && npm run compile:release:secure-frame-front-end"
  },
  "author": "",
  "license": "PROPRIETARY",
  "devDependencies": {
    "typescript": "^4.4.2"
  }
}
