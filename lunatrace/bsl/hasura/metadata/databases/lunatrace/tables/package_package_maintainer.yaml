table:
  schema: package
  name: package_maintainer
insert_permissions:
  - role: service
    permission:
      check: {}
      columns:
        - maintainer_id
        - package_id
      backend_only: false
select_permissions:
  - role: service
    permission:
      columns:
        - maintainer_id
        - package_id
      filter: {}
update_permissions:
  - role: service
    permission:
      columns:
        - maintainer_id
        - package_id
      filter: {}
      check: {}
