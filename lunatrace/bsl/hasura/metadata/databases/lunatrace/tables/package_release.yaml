table:
  schema: package
  name: release
insert_permissions:
  - role: service
    permission:
      check: {}
      columns:
        - upstream_data
        - blob_hash
        - mirrored_blob_url
        - upstream_blob_url
        - version
        - observed_time
        - release_time
        - id
        - package_id
        - publishing_maintainer_id
      backend_only: false
select_permissions:
  - role: service
    permission:
      columns:
        - upstream_data
        - blob_hash
        - mirrored_blob_url
        - upstream_blob_url
        - version
        - observed_time
        - release_time
        - id
        - package_id
        - publishing_maintainer_id
      filter: {}
update_permissions:
  - role: service
    permission:
      columns:
        - upstream_data
        - blob_hash
        - mirrored_blob_url
        - upstream_blob_url
        - version
        - observed_time
        - release_time
        - id
        - package_id
        - publishing_maintainer_id
      filter: {}
      check: {}
