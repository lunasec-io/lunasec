table:
  name: build_dependency_relationship
  schema: public
object_relationships:
  - name: depended_by_relationship
    using:
      foreign_key_constraint_on: depended_by_relationship_id
  - name: release
    using:
      foreign_key_constraint_on: release_id
computed_fields:
  - name: recursive_flat
    definition:
      function:
        name: build_dependency_relationship_recursive
        schema: public
insert_permissions:
  - role: service
    permission:
      check: {}
      columns:
        - depended_by_relationship_id
        - id
        - release_id
select_permissions:
  - role: service
    permission:
      columns:
        - depended_by_relationship_id
        - id
        - release_id
      filter: {}
  - role: user
    permission:
      columns:
        - depended_by_relationship_id
        - id
        - release_id
      filter: {}
update_permissions:
  - role: service
    permission:
      columns:
        - depended_by_relationship_id
        - id
        - release_id
      filter: {}
      check: {}
delete_permissions:
  - role: service
    permission:
      backend_only: false
      filter: {}
