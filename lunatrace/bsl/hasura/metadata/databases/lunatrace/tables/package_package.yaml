table:
  schema: package
  name: package
configuration:
  custom_root_fields: {}
  custom_name: package
  column_config: {}
  custom_column_names: {}
array_relationships:
  - name: package_maintainers
    using:
      foreign_key_constraint_on:
        column: package_id
        table:
          schema: package
          name: package_maintainer
  - name: releases
    using:
      foreign_key_constraint_on:
        column: package_id
        table:
          schema: package
          name: release
insert_permissions:
  - role: service
    permission:
      check: {}
      columns:
        - custom_registry
        - description
        - name
        - package_manager
        - id
      backend_only: false
select_permissions:
  - role: service
    permission:
      columns:
        - custom_registry
        - description
        - name
        - package_manager
        - id
      filter: {}
update_permissions:
  - role: service
    permission:
      columns:
        - custom_registry
        - description
        - name
        - package_manager
        - id
      filter: {}
      check: {}
