table:
  schema: package
  name: maintainer
insert_permissions:
  - role: service
    permission:
      check: {}
      columns:
        - email
        - name
        - package_manager
        - id
      backend_only: false
select_permissions:
  - role: service
    permission:
      columns:
        - email
        - name
        - package_manager
        - id
      filter: {}
update_permissions:
  - role: service
    permission:
      columns:
        - email
        - name
        - package_manager
        - id
      filter: {}
      check: {}
