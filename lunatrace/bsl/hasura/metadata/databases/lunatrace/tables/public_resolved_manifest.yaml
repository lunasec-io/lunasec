table:
  name: resolved_manifest
  schema: public
array_relationships:
  - name: manifest_dependencies
    using:
      foreign_key_constraint_on:
        column: manifest_id
        table:
          name: manifest_dependency
          schema: public
