table:
  schema: package
  name: release_dependency
insert_permissions:
  - role: service
    permission:
      check: {}
      columns:
        - package_name
        - package_version_query
        - dependency_package_id
        - dependency_release_id
        - id
        - release_id
      backend_only: false
select_permissions:
  - role: service
    permission:
      columns:
        - package_name
        - package_version_query
        - dependency_package_id
        - dependency_release_id
        - id
        - release_id
      filter: {}
update_permissions:
  - role: service
    permission:
      columns:
        - package_name
        - package_version_query
        - dependency_package_id
        - dependency_release_id
        - id
        - release_id
      filter: {}
      check: {}
