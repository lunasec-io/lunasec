SELECT * FROM public.organizations;
-- This is a fake down migration to make everything work.  We don't actually want to take that enum type off of the source_type enum because that will not work so..noop
