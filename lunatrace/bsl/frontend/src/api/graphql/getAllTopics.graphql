query GetAllTopics {
    topics(order_by: {created_at: desc}) {
        created_at
        id
        metadata
        metadata_schema_version
        severity
        summary
        tags
        title
        topic_unique_id
        updated_at
    }
}
