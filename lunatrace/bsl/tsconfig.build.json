// TODO: this is currently broken due to some strange PNP/Typescript incompatibility, I believe
// It works if you compile ONLY the backend first by CDing into it
// the issue is with @envelop/types which is a subdep of graphql yoga
{
  "files": [],
  "references": [
    { "path": "backend" },
    { "path": "frontend"},
    { "path": "common" },
    { "path": "logger" }
  ]
}
