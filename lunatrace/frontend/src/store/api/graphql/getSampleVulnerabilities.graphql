query SampleVulnerabilities {
    vulnerabilities(limit: 15) {
        id
        name
        namespace
        record_source
        severity
        cvss_score
        cvss_inferred
        created_at
        description
        slug
        data_source
        vulnerability_packages {
            name
            id
            slug
        }
    }
}
