table:
  name: sboms
  schema: public
array_relationships:
- name: builds
  using:
    foreign_key_constraint_on:
      column: sbom_id
      table:
        name: builds
        schema: public
- name: scans
  using:
    foreign_key_constraint_on:
      column: sbom_id
      table:
        name: scans
        schema: public
